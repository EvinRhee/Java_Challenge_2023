//import java.util.ArrayList;

//public class Die 
{
   // int numSides;
   // boolean lastRoll = false;
   // ArrayList<String> number  = new ArrayList<String>();

   // public static void main(String[] args)
   // {
    //    Die x = new Die();
    //    System.out.println(x.die);
    //    System.out.println(x.roll);
    
   // }
/* 
    public die()
    {
        int numSides = 6;
    }

    public die(int n)
    {
        int numSides = n;
    }

    int numSides()
    {
        return numSides();
    }

    int roll()
    {
        for (int f = 0; f < 1; f++)
        {
            int max = numSides;
            int min = 1;
            int range = max - min + 1;
            lastRoll = true;
            int random = (int)(Math.random() * range) + min;
            l1.add(roll);
            System.out.println("Roll" + random + " = " random);
        
        }
    }

    int readLastRoll()
    {
        if(lastRoll == true)
        {
            System.out.println("Last roll was " + l1.get(0));
        }
        else
        {
            System.out.println("Roll a die first");
            return -1;
        }
    }

 */   
    
}